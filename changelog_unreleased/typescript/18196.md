#### Fix method chain breaking (#18196 by @fisker)

<!-- prettier-ignore -->
```tsx
// Input
const model = types
  .model({ something: mxSomething })
  .volatile<{
    loading: boolean;
    savingStatus: "idle" | "saving" | "saved" | "error";
    undoDisabled: boolean;
    online: boolean;
  }>(self => ({
    loading: false,
    savingStatus: "idle",
    undoDisabled: false,
    aiFocused: false,
    online: true,
  }));

// Prettier stable (--object-wrap=collapse)
const model = types
  .model({ something: mxSomething })
  .volatile<{
    loading: boolean;
    savingStatus: "idle" | "saving" | "saved" | "error";
    undoDisabled: boolean;
    online: boolean;
  }>((self) => ({ loading: false, savingStatus: "idle", undoDisabled: false, aiFocused: false, online: true }));

// Prettier main (--object-wrap=collapse)
const model = types
  .model({ something: mxSomething })
  .volatile<{
    loading: boolean;
    savingStatus: "idle" | "saving" | "saved" | "error";
    undoDisabled: boolean;
    online: boolean;
  }>(self => ({
    loading: false,
    savingStatus: "idle",
    undoDisabled: false,
    aiFocused: false,
    online: true,
  }));
```
