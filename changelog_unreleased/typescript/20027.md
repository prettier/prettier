#### Add missing parentheses for `TSInstantiationExpression` (#20027 by @fisker)

<!-- prettier-ignore -->
```tsx
// Input
new (a?.b<h>)();
(a?.b<c>)`foo`;

// Prettier stable
new a?.b<h>();
a?.b<c>`foo`;

// Prettier main
new (a?.b<h>)();
(a?.b<c>)`foo`;
```
