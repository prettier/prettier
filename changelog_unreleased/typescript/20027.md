#### Add missing parentheses for `TSInstantiationExpression` (#20027 by @fisker)

<!-- prettier-ignore -->
```tsx
// Input
new (f?.g<h>)();
(a?.b<c>)`foo`;

// Prettier stable
new f?.g<h>();
a?.b<c>`foo`;

// Prettier main
new (f?.g<h>)();
(a?.b<c>)`foo`;
```
