#### Fix comment inside union type get duplicated (#18393 by @fisker)

<!-- prettier-ignore -->
```tsx
// Input
type Foo = (/** comment */ a | b) | c;

// Prettier stable
type Foo = /** comment */ (/** comment */ a | b) | c;

// Prettier main
type Foo = /** comment */ (a | b) | c;
```
