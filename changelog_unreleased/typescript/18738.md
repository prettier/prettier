#### Print semicolon before type assertion in `no-semi` mode (#18738 by @fisker)

<!-- prettier-ignore -->
```tsx
// Input
a;
<b>c;

// Prettier stable (First format, --no-semi)
a
<b>c

// Prettier stable (Second format, --no-semi)
a < b > c

// Prettier main
a
;<b>c
```
