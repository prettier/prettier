#### Fix unstable comment in the parentheses of the first intersection member (#20132 by @HuzaifaChaudary)

<!-- prettier-ignore -->
```tsx
// Input
type A = ( // comment
  | Foo
  | Bar
) & Baz;

// Prettier stable (first format)
type A = // comment
(Foo | Bar) &
  Baz;

// Prettier stable (second format)
type A =
  // comment
  (Foo | Bar) & Baz;

// Prettier main
type A =
  // comment
  (Foo | Bar) & Baz;
```
