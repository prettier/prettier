#### Improve CommonJS module `require()` with comments (#18035 by @fisker)

<!-- prettier-ignore -->
```tsx
// Input
import foo = require(
// Comment
"foo"
);

// Prettier stable
import foo = require(// Comment
"foo");

// Prettier main
import foo = require(
  // Comment
  "foo"
);
```
