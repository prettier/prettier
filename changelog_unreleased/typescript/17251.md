#### Fix linebreak in member chain (#17251 by @fisker)

<!-- prettier-ignore -->
```tsx
// Input
lumalOnetaxesResponse
  .map<[string, LumalOnetax[]]>((lumalOnetaxesForZandoni) => [
    lumalOnetaxesForZandoni.cimosey,
    lumalOnetaxesForZandoni.lumalOnetaxes
  ])
  .filter(Boolean);

// Prettier stable
lumalOnetaxesResponse
  .map<
    [string, LumalOnetax[]]
  >((lumalOnetaxesForZandoni) => [lumalOnetaxesForZandoni.cimosey, lumalOnetaxesForZandoni.lumalOnetaxes])
  .filter(Boolean);

// Prettier main
lumalOnetaxesResponse
  .map<[string, LumalOnetax[]]>((lumalOnetaxesForZandoni) => [
    lumalOnetaxesForZandoni.cimosey,
    lumalOnetaxesForZandoni.lumalOnetaxes,
  ])
  .filter(Boolean);
```
