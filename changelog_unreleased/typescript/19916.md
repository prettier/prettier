#### Fix line break before `!` after a comment in member chains (#19916 by @kovsu)

<!-- prettier-ignore -->
```tsx
// Input
obj.aaa /* c */!.substring(0);

// Prettier stable
obj
  .aaa /* c */
  !.substring(0);

// Prettier main
obj
  .aaa /* c */!
  .substring(0);
```
