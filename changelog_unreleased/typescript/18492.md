#### Enhance binary cast expression handling and add tests for trace entrypoints (#18492 by @williamquintas)

<!-- Optional description if it makes sense. -->

<!-- prettier-ignore -->
```tsx
// Input
new ((
  require("./webpack/plugins/next-trace-entrypoints-plugin")
)
  .TraceEntryPointsPlugin as P)({
  rootDir: dir,
});

// Prettier stable
new (require("./webpack/plugins/next-trace-entrypoints-plugin")
  .TraceEntryPointsPlugin as P)({
  rootDir: dir,
});

// Prettier main
new (
  require("./webpack/plugins/next-trace-entrypoints-plugin")
    .TraceEntryPointsPlugin as P
)({
  rootDir: dir,
});
```


