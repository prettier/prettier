#### Improve CLI performance for simple ignored directories (#19153 by @Sean-Kenneth-Doherty)

Prettier now prunes simple ignore-file entries during directory traversal in `--write`, `--check`, `--list-different`, and `--debug-check` modes, while keeping the existing ignore filter as the source of truth.
