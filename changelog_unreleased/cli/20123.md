#### Separate CLI option normalization from core validation (#20123 by @saitakarcesme)

CLI-specific handling of aliases, repeated flags, positional arguments and value coercion now lives in the CLI layer. API validation and command-line behavior are unchanged.
