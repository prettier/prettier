#### Preserve empty line before `;` in `no-semi` mode (#18736, #18737 by @fisker)

<!-- prettier-ignore -->
```jsx
// Input
a

;[].b()

// Prettier stable (--no-semi)
a
;[].b()

// Prettier main
a

;[].b()
```
