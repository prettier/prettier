#### Preserve quote for embedded HTML attribute values (#18352 by @kovsu)

<!-- prettier-ignore -->
```tsx
// Input
const html = /* HTML */ ` <div class="${styles.banner}"></div> `;

// Prettier stable
const html = /* HTML */ ` <div class=${styles.banner}></div> `;

// Prettier main
const html = /* HTML */ ` <div class="${styles.banner}"></div> `;
```
