#### Fix unstable comment before a parenthesized ternary test (#20131 by @HuzaifaChaudary)

<!-- prettier-ignore -->
```jsx
// Input
a = {
  b: (
    // Comment
    c
  ) ? d : e,
};

// Prettier stable
a = {
  b: // Comment
  c
    ? d
    : e,
};

// Prettier stable (second format)
a = {
  // Comment
  b: c ? d : e,
};

// Prettier main
a = {
  b:
    // Comment
    c ? d : e,
};
```
