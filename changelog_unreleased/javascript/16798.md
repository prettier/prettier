#### Fix if-else formatting for JSDoc type casting (#16798 by @sosukesuzuki)

<!-- prettier-ignore -->
```jsx
// Input
if (foo) a = b
else /**@type {string}*/ (foo).split

// Prettier stable
if (foo) a = b;
/**@type {string}*/ else (foo).split;

// Prettier main
if (foo) a = b
else /**@type {string}*/ (foo).split
```
