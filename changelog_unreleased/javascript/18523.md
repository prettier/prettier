#### Fix moved line comment in member chains with a short object name (#18523 by @snmsoodan)

A line comment after the object of a member chain was printed at the end of the whole chain when the object name was short enough to be merged with the next group.

<!-- prettier-ignore -->
```jsx
// Input
a //
  .bla();

// Prettier stable
a.bla(); //

// Prettier main
a //
  .bla();
```
