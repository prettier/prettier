#### Remove unexpected space inserted before expression in CSS selectors (#18460 by @kovsu)

<!-- prettier-ignore -->
```js
// Input
css`foo:${bar} {}`

// Prettier stable
css`
  foo: ${bar} {

  }
`;

// Prettier main
css`
  foo:${bar} {
  }
`;
```
