#### Fix comment duplication for experimental ternaries (#15965 by @sosukesuzuki)

<!-- prettier-ignore -->
```jsx
// Input
x =
  IS_AUTOTEST || (IS_DEV && !IS_TEST) ?
    [
      /*FooBlock*/
    ]
  : [];

// Prettier stable
x =
  IS_AUTOTEST || (IS_DEV && !IS_TEST) ?
    /*FooBlock*/
    [
      /*FooBlock*/
    ]
  : [];

// Prettier main
x =
  IS_AUTOTEST || (IS_DEV && !IS_TEST) ?
    [
      /*FooBlock*/
    ]
  : [];

```
