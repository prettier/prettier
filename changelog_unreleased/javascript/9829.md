#### Fix comments handling for for-statements that body is expression statements (#9829 by @sosukesuzuki)

<!-- prettier-ignore -->
```js
// Input
for (x of y)
  // comment
  bar();

// Prettier stable
// comment
for (x of y) bar();

// Prettier master
for (x of y)
  // comment
  bar();
```
