#### Avoid double parentheses around logical expression (#18397 by @fisker)

<!-- prettier-ignore -->
```jsx
// Input
const a = !(x || y /* foo */);

// Prettier stable
const a = !((x || y) /* foo */);

// Prettier main
const a = !(x || y /* foo */);
```
