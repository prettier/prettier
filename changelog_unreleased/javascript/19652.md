#### Fix misplaced type cast comments (#19652 by @fisker)

<!-- prettier-ignore -->
```jsx
// Input
foo = a ? b : (/** @type {Foo} */ (foo) ?? bar);

// Prettier stable
foo = a ? b : /** @type {Foo} */ ((foo) ?? bar);

// Prettier main
foo = a ? b : (/** @type {Foo} */ (foo) ?? bar);
```
