#### Improve `require()` call with comments (#18037 by @fisker)

<!-- prettier-ignore -->
```jsx
// Input
require(
// Comment
"foo"
);

call(
// Comment
"foo"
);

// Prettier stable
require(// Comment
"foo");

call(
  // Comment
  "foo",
);

// Prettier main
require(
  // Comment
  "foo",
);

call(
  // Comment
  "foo",
);
```
