#### Fix unstable comment in arrow function with sequence expression body (#19253 by @itsybitsybootsy)

<!-- prettier-ignore -->
```jsx
// Input
() => (a, b, c /* abc */);

// Prettier stable
() => (a, b, c) /* abc */;

// Prettier main
() => (a, b, c /* abc */);
```
