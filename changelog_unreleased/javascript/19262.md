#### Fix unstable comment in arrow function with assignment expression body (#19262 by @itsybitsybootsy)

<!-- prettier-ignore -->
```jsx
// Input
const fn = () => (a = b /* abc */);

// Prettier stable
const fn = () => (a = b) /* abc */;

// Prettier main
const fn = () => (a = b /* abc */);
```
