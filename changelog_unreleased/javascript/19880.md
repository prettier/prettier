#### Fix range formatting expanding to a non-source-element node (#19880 by @Kjubikstronk)

<!-- prettier-ignore -->
```jsx
// Input (range covers the two indented lines)
beforeEach(() => {
  // c
  a();
});

// Prettier stable
beforeEach(() => {
  // c
  a();
};);

// Prettier main
beforeEach(() => {
  // c
  a();
});
```
