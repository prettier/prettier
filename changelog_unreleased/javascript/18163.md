#### Preserve parentheses for BinaryExpression with bitwise parent operators (#18163 by @fs0414)

<!-- prettier-ignore -->
```js
// Input
1 << (bit % 8);
1 >> (bit - 8);

// Prettier stable
1 << bit % 8;
1 >> (bit - 8);

// Prettier main
1 << (bit % 8);
1 >> (bit - 8);
```
