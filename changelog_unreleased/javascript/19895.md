#### Fix unstable own line comment in a for-in or for-of head (#19895 by @Kjubikstronk)

<!-- prettier-ignore -->
```jsx
// Input
for (x
//c
in y);

// Prettier stable (first format)
for (x in //c
y);

// Prettier stable (second format)
for (x in y); //c

// Prettier main
for (x in y);
//c
```
