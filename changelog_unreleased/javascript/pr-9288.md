#### Fix object destructuring inside `for-of` loop (#9288 by @fisker)

<!-- prettier-ignore -->
```js
// Input
for({a: {b}} of c){}

// Prettier stable
for ({
  a: { b },
} of c) {
}

// Prettier master
for ({ a: { b } } of c) {
}
```
