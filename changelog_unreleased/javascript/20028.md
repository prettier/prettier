#### Keep necessary parentheses around `throw` expression (#20028 by @lazerg)

<!-- prettier-ignore -->
```jsx
// Input
(throw error);

// Prettier stable
throw error;

// Prettier main
(throw error);
```
