#### Fix unstable comments in arrow function bodies (#19181 by @Poliklot)

<!-- prettier-ignore -->
```jsx
// Input
() => (a, b, c /* abc */);

// Prettier stable
() => (a, b, c) /* abc */;

// Prettier main
() => (a, b, c /* abc */);
```

<!-- prettier-ignore -->
```tsx
// Input
const value = () =>
  (
    // eslint-disable-next-line @typescript-eslint/no-unsafe-type-assertion
    (foo.bar()) as Foo
  ).baz;

// Prettier stable second output
const value = () =>
  // eslint-disable-next-line @typescript-eslint/no-unsafe-type-assertion
  (foo.bar() as Foo).baz;

// Prettier main
const value = () =>
  (
    // eslint-disable-next-line @typescript-eslint/no-unsafe-type-assertion
    foo.bar() as Foo
  ).baz;
```
