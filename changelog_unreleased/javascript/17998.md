#### Improve comment handing between if statement condition and the "body" (#XXXX by @user)

<!-- prettier-ignore -->
```jsx
// Input
if (foo) // comment
{
  doThing(foo);
}

if (foo)
/* comment */
{
  doThing(foo);
}

// Prettier stable
if (foo) {
  // comment
  doThing(foo);
}

if (foo) {
  /* comment */
  doThing(foo);
}

// Prettier main
if (foo) // comment
{
  doThing(foo);
}

if (foo) /* comment */
{
  doThing(foo);
}
```
