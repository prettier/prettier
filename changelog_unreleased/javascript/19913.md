#### Keep necessary parentheses in `yield` expression (#19913 by @giaBaoJS)

<!-- prettier-ignore -->
```jsx
// Input
function* f() {
  yield (/*
  */ a);
}

// Prettier stable
function* f() {
  yield /*
   */ a;
}

// Prettier main
function* f() {
  yield (
    /*
     */ a
  );
}
```
