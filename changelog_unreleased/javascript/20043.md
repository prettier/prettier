#### Preserve comments in test calls (#20043 by @lllleolin-max)

<!-- prettier-ignore -->
```jsx
// Input
test("x", () => {
  run();
}, // first
// second
// third
60000);

// Prettier stable
test("x", () => {
  run();
}, // second // first
// third
60000);

// Prettier main
test(
  "x",
  () => {
    run();
  }, // first
  // second
  // third
  60000,
);
```
