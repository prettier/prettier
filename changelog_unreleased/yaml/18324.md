#### Use explicit key style for flow-mapping keys with trailing comments (#18324 by @kovsu, @fisker)

<!-- prettier-ignore -->
```yaml
# Input
{"foo": #comment
bar}

# Prettier stable
{ "foo": bar } #comment

# Prettier main
{ ? "foo" # comment
  : bar }
```
