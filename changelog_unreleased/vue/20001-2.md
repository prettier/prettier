#### Avoid adding blank lines to interpolations (#20001 by @vzer200)

<!-- prettier-ignore -->
```html
<!-- Input -->
<template>
  <div>
    ({{ a
    }})
  </div>
</template>

<!-- Prettier stable (--embedded-language-formatting=off) -->
<template>
  <div>
    ({{ a

    }})
  </div>
</template>

<!-- Prettier main (--embedded-language-formatting=off) -->
<template>
  <div>
    ({{ a
    }})
  </div>
</template>
```
