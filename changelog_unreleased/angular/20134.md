#### Avoid breaking inline element closing tags after Angular interpolations (#20134 by @splincode)

<!-- prettier-ignore -->
```html
<!-- Input -->
<div class="text">
  Sorry, we couldn’t find an account for <b>{{ phoneInput }}</b
  >, let's create one:
</div>

<!-- Prettier stable -->
<div class="text">
  Sorry, we couldn’t find an account for <b>{{ phoneInput }}</b
  >, let's create one:
</div>

<!-- Prettier main -->
<div class="text">
  Sorry, we couldn’t find an account for <b>{{ phoneInput }}</b>, let's create
  one:
</div>
```
