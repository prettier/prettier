#### Support expression for exhaustive typechecking (#19571 by @fisker)

<!-- prettier-ignore -->
```html
<!-- Input -->
@switch (state.mode) {
  @default never(state);
}

<!-- Prettier stable -->
@switch (state.mode) {
  @default never;
}

<!-- Prettier main -->
@switch (state.mode) {
  @default never(state);
}
```
