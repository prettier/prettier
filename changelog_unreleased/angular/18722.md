#### Support Angular v21.2 (#18722 by @fisker)

<!-- prettier-ignore -->
```html
<!-- Input -->
@let fn = (a) =>        a?    1:2;

{{ fn ( a         instanceof b)}}

<!-- Prettier stable -->
@let fn = (a) =>        a?    1:2;

{{ fn ( a         instanceof b)}}

<!-- Prettier main -->
@let fn = (a) => (a ? 1 : 2);

{{ fn(a instanceof b) }}
```
