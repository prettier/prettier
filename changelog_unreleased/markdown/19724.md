#### Preserve Liquid blocks after Markdown tables (#19724 by @al-ameen)

Standalone Liquid tags are now parsed as block syntax, preventing multiline tags from being absorbed into a preceding table or blockquote. Whitespace inside multiline Liquid tags is also now preserved.

<!-- prettier-ignore -->
```markdown
<!-- Input -->
| Argument | Type |
| -------- | ---- |
{% set default_params = {
    "model": ["str"],
} %}

<!-- Prettier stable -->
| Argument                  | Type |
| ------------------------- | ---- |
| {% set default_params = { |

    "model": ["str"],

} %}

<!-- Prettier main -->
| Argument | Type |
| -------- | ---- |
{% set default_params = {
    "model": ["str"],
} %}
```
