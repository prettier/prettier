#### Remove extra spaces after `[$` in indirect property lookups (#19782 by @kovsu)

<!-- prettier-ignore -->
```less
// Input
.foo {
  value: #namespace[$@prop-name];
}

// Prettier stable
.foo {
  value: #namespace[$ @prop-name];
}

// Prettier main
.foo {
  value: #namespace[$@prop-name];
}
```
