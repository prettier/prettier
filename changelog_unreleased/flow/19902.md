#### Fix comment in destructuring patterns (#19902 by @fisker)

<!-- prettier-ignore -->
```flow
// Input
const {
  foo,
  // bar
  // baz
}: Foo = expr;

// Prettier stable (First format)
const {
  foo1,
} // bar
// baz
: Foo = expr;

// Prettier stable (Second format)
const {
  foo1, // bar
} // baz
: Foo = expr;

// Prettier main
const {
  foo1,
  // bar
  // baz
}: Foo = expr;
```
