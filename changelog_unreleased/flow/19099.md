#### Add support for `writeonly` variance modifier (#19099 by @marcoww6)

<!-- prettier-ignore -->
```jsx
// Input
type T = {writeonly foo: string};

// Prettier stable
// SyntaxError

// Prettier main
type T = { writeonly foo: string };
```
