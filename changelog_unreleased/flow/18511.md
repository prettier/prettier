#### Add support for Flow match instance patterns and Flow records (#18511 by @gkz)

<!-- prettier-ignore -->
```jsx
// Input
record R {
  num: number,
}

const x = R {num: 1};

const label = match (x) {
  R {num: 0} => "zero",
  R {num: 1} => "one",
  R {const num} => `${num} items`,
}

// Prettier stable
// Unsupported

// Prettier main
// Same as input
```
