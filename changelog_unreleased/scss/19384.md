#### Remove space before `;` delimiter in SCSS `if()` function (#19384 by @kovsu)

<!-- prettier-ignore -->
```scss
// Input
$value: if(sass($x): 1 ; else: 2);

// Prettier stable
$value: if(sass($x): 1 ; else: 2);

// Prettier main
$value: if(sass($x): 1; else: 2);
```
