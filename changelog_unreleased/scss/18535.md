#### Preserve line breaks for inline comments in SCSS maps (#18535 by @kovsu)

<!-- prettier-ignore -->
```scss
// Input
$map: (
  // comment
);

// Prettier stable
$map: (// comment);

// Prettier main
$map: (
  // comment
);
```
