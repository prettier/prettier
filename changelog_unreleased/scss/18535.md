#### Fix comment print in maps (#18535 by @kovsu)

<!-- prettier-ignore -->
```scss
// Input
$map: (
  // comment
);

// Prettier stable
$map: (// comment);

// Prettier main
$map: (
  // comment
);
```
