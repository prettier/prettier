#### Prevent trailing comma in `if()` function (#18471 by @kovsu)

<!-- prettier-ignore -->
```scss
// Input
$value: if(sass(false): 1; else: -1);

// Prettier stable
$value: if(
  sass(false): 1; else: -1,
);

// Prettier main
$value: if(sass(false): 1; else: -1);
```
