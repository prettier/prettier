#### Preserve empty lines after function calls in parenthesized values (#19960 by @kovsu)

<!-- prettier-ignore -->
```css
/* Input */
a {
  background: linear-gradient(
    to right,
    rgba(0, 0, 0, 0.5) calc(50% - 1px),

    transparent calc(50% + 1px)
  );
}

/* Prettier stable */
a {
  background: linear-gradient(
    to right,
    rgba(0, 0, 0, 0.5) calc(50% - 1px),
    transparent calc(50% + 1px)
  );
}

/* Prettier main */
a {
  background: linear-gradient(
    to right,
    rgba(0, 0, 0, 0.5) calc(50% - 1px),

    transparent calc(50% + 1px)
  );
}
```
