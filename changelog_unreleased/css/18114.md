#### Remove unexpected space between font size and line height (#18114 by @kovsu)

<!-- prettier-ignore -->
```css
/* Input */
a {
  font: var(--size)/1;
}

/* Prettier stable */
a {
  font: var(--size) / 1;
}

/* Prettier main */
a {
  font: var(--size)/1;
}
```
