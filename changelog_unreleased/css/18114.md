#### Remove space between font family and line height (#18114 by @kovsu)

<!-- prettier-ignore -->
```css
/* Input */
a {
  font: var(--size)/1;
  font: var(--size) / 1;
}

/* Prettier stable */
a {
  font: var(--size) / 1;
  font: var(--size) / 1;
}

/* Prettier main */
a {
  font: var(--size)/1;
  font: var(--size) / 1;
}
```
