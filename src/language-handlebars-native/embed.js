// Embed function for handlebars - can be used to embed other languages
// within handlebars templates (e.g., CSS in style blocks, JS in script blocks)

function embed(/* path, print, textToDoc, options */) {
  // For now, return undefined to indicate no embedding
  // This can be expanded later to handle embedded languages
  return null;
}

export default embed;
