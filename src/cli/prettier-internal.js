import { __internal as sharedWithCli } from "../index.js";

export const {
  errors,
  optionCategories,
  createIgnorer,
  optionsHiddenDefaults,
  normalizeOptions,
  getSupportInfoWithoutPlugins,
  vnopts,
  fastGlob,
} = sharedWithCli;
