// Fixture for tests/integration/__tests__/ignored-glob.js
// NOT ignored and already formatted. Drives the "some but not all ignored" case
// of the `**/*.js` glob test, where the presence of this file suppresses the warning.
const kept = 1;
