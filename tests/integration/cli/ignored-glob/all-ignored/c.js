// Fixture for tests/integration/__tests__/ignored-glob.js
// In an ignored dir (`all-ignored/`). The test passes the directory itself
// (`prettier all-ignored`), which must NOT warn -- guarding `prettier --check .`.
const c=3
