// Fixture for tests/integration/__tests__/ignored-glob.js
// Ignored via .prettierignore (`ignored/`). Matched by the `ignored/*.js` glob
// test, where every match is ignored -> Prettier should warn.
const a=1
