"use strict";

module.exports = {
  languages: [
    {
      name: "bespoke",
      parsers: ["bespoke"],
    },
  ],
  parsers: {
    bespoke: {
      parse: text => ({ text }),
      astFormat: "bespoke"
    },
  },
  printers: {
    bespoke: {
      print(path) {
        return ["bespoke+", path.getValue().text];
      },
    },
  },
};
