"use strict";

module.exports = {
  languages: [
    {
      name: "foo",
      parsers: ["foo"],
      extensions: [".foo"],
    },
  ],
  parsers: {
    foo: {
      parse: text => ({ text }),
      astFormat: "foo",
    },
  },
  printers: {
    foo: {
      print(path) {
        return ["foo+", path.getValue().text];
      },
    },
  },
};
