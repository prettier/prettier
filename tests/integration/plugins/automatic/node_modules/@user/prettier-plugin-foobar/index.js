"use strict";

module.exports = {
  languages: [
    {
      name: "foobar",
      parsers: ["foobar"],
      extensions: [".foobar"],
    },
  ],
  parsers: {
    foobar: {
      parse: text => ({ text }),
      astFormat: "foobar",
    },
  },
  printers: {
    foobar: {
      print(path) {
        return ["foobar+", path.getValue().text];
      },
    },
  },
};
