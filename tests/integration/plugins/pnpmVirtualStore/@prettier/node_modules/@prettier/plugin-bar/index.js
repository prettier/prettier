"use strict";

module.exports = {
  languages: [
    {
      name: "bar",
      parsers: ["bar"],
      extensions: [".bar"],
    },
  ],
  parsers: {
    bar: {
      parse: text => ({ text }),
      astFormat: "bar",
    },
  },
  printers: {
    bar: {
      print(path) {
        return ["bar+", path.getValue().text];
      },
    },
  },
};
