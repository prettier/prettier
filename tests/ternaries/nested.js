const value = condition1
  ? value1
  : condition2
      ? value2
      : condition3
          ? value3
          : value4;

const value =
  condition1 ? value1 :
  condition2 ? value2 :
  condition3 ? value3 :
  value4;
