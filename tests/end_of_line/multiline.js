const foo = bar(
  (_0: baz) => {
    setTimeout(() => {
      /*
          Multiline comment
          Multiline comment
          Multiline comment
      */
      console.log(
        'Multiline string\
         Multiline string\
         Multiline string'
      );
    });
  }
);
