// @flow

// resolves to node_modules/bar/index.js even though bar/package.json is invalid
require('bar');

// can't be resolved because node_modules/foo/package.json is invalid and there
// is no default "main" file (index.js)
require('foo');
