/**
 * @format
 * @flow
 */

{
  const x = 42;
  // empty line
  const y = 42;
  ([x, y]: [empty, empty]);
  // empty line
  // empty line
  // empty line
  // empty line
  // empty line
  ([x, y]: [empty, empty]);
  ([x, y]: [empty, empty]);
}

{
  const x = 42;
  // empty line
  // empty line
  // empty line
  const y = 42;
  ([x, y]: [empty, empty]);
  // empty line
  // empty line
  // empty line
  // empty line
  // empty line
  ([x, y]: [empty, empty]);
  ([x, y]: [empty, empty]);
}

{
  const x = 42;
  // empty line
  // empty line
  // empty line
  // empty line
  const y = 42;
  ([x, y]: [empty, empty]);
  // empty line
  // empty line
  // empty line
  // empty line
  // empty line
  ([x, y]: [empty, empty]);
  ([x, y]: [empty, empty]);
}
