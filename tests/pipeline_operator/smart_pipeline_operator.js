a |> await # |> # * 3;

foo
  |> await #
  |> # || throw new Error(`foo ${bar1}`)
  |> bar2(#, ", ")
  |> bar3
  |> # + "!"
  |> new Bar.Foo(#)
  |> await bar.bar(#)
  |> console.log;

const result = "hello"
  |> doubleSay
  |> capitalize(#, "foo")
  |> exclaim;

function createPerson (attrs) {
  attrs
    |> foo
    |> foo
    |> Person.insertIntoDatabase;
}
