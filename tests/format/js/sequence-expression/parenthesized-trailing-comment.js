const declared = (a, b, c /* comment */);

(a, b, c /* comment */);

assigned = (a, b, c /* comment */);

function f() {
  return (a, b, c /* comment */);
}

const assignInit = (m = n /* comment */);

function g() {
  return (p = q /* comment */);
}
