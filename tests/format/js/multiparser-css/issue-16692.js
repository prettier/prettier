const c1 = styled.div`
    border:    1px     ${solid}      red;
`;

const c2 = styled.div`
 font-size:      var(--font-size-h${level});
`;

const c3 = styled.div`
    grid-area:       area-${area};
`;

const c4 = styled.div`
    grid-area:       var(--${one});
`;

const c5 = styled.div`
 font-size:      var(--font-size-h ${level});
`;
