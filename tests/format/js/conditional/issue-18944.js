condition ? ifTrue
: [
      // Hello, world!
  ];

condition ? [
      // Hello, world!
  ]
: ifFalse;

condition ? ifTrue
: {
      // Hello, world!
  };

condition ? {
    // Hello, world!
  }
: ifFalse;

condition ? ifTrue
: [
      // Hello, world!
    1,
  ];
