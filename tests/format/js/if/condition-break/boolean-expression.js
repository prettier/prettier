if (Boolean(
  node.type === "ImportExpression" ||
  node.type === "TSImportType" ||
  node.type === "TSExternalModuleReference"));

while (Boolean(
  node.type === "ImportExpression" &&
  node.type === "TSImportType" &&
  node.type === "TSExternalModuleReference"));

do {} while (Boolean(
  node.type === "ImportExpression" ??
  node.type === "TSImportType" ??
  node.type === "TSExternalModuleReference"));

if (!!!Boolean(
  node.type === "ImportExpression" ||
  node.type === "TSImportType" ||
  node.type === "TSExternalModuleReference"));

if (!Boolean(!!Boolean(
  node.type === "ImportExpression" ||
  node.type === "TSImportType" ||
  node.type === "TSExternalModuleReference")));

if ( // comment
Boolean(
  node.type === "ImportExpression" ||
  node.type === "TSImportType" ||
  node.type === "TSExternalModuleReference"));

if (Boolean // comment
(
  node.type === "ImportExpression" ||
  node.type === "TSImportType" ||
  node.type === "TSExternalModuleReference"));

if (Boolean?.
(
  node.type === "ImportExpression" ||
  node.type === "TSImportType" ||
  node.type === "TSExternalModuleReference"));

