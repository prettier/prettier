foo = /** @type A */ ({
  // comment
  width,
  height,
  ...margins,
});

foo = /** @type A */ (#{
  // comment
  width,
  height,
  ...margins,
});

foo = /** @type A */ ([
  // comment
  width,
  height,
  ...margins,
]);

foo = /** @type A */ (#[
  // comment
  width,
  height,
  ...margins,
]);
