function Foo(
  bar
  // Trailing comment
) {}

function Foo(
  {bar}
  // Trailing comment
) {}

function Foo(
  [bar]
  // Trailing comment
) {}

function Foo(
  bar = 1
  // Trailing comment
) {}

function Foo(
  ...bar
  // Trailing comment
) {}
