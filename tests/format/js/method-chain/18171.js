callExprssion(
  instance.capabilities.get(TerminalCapability?.PromptTypeDetection)
    ?.promptType,
);

optionalCallExprssion?.(
  instance.capabilities.get(TerminalCapability?.PromptTypeDetection)
    ?.promptType,
);

new NewExpression(
  instance.capabilities.get(TerminalCapability?.PromptTypeDetection)
    ?.promptType,
);
