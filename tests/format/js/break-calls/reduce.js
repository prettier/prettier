const [ first1 ] = array.reduce(
  () => [accumulator, element, accumulator, element],
  [fullName]
);

const [ first2 ] = array.reduce(
  (accumulator, element, ) => [accumulator, element],
  [fullName]
);
