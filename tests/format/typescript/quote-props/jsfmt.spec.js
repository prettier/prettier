run_spec(__dirname, ["typescript"], {
  quoteProps: "as-needed",
});

run_spec(__dirname, ["typescript"], {
  quoteProps: "preserve",
});

run_spec(__dirname, ["typescript"], {
  quoteProps: "consistent",
});
