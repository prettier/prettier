run_spec(import.meta, ["typescript"], {
  quoteProps: "as-needed",
});

run_spec(import.meta, ["typescript"], {
  quoteProps: "preserve",
});

run_spec(import.meta, ["typescript"], {
  quoteProps: "consistent",
});
