transform(
  // prettier-ignore
  (pointTransformer: T)
);

transform(
  // prettier-ignore
  (/* comment */pointTransformer: T /* comment */)
);

transform(
  /* prettier-ignore */(/* prettier-ignore */pointTransformer: (Point => Point))
);
