"use strict";

const concat = require("../../../../../../src/doc").builders.concat;

module.exports = {
  languages: [
    {
      name: "bespoke",
      parsers: ["bespoke"]
    }
  ],
  parsers: {
    bespoke: {
      parse: text => ({ text }),
      astFormat: "bespoke"
    }
  },
  printers: {
    bespoke: {
      print: path => concat(["bespoke+", path.getValue().text])
    }
  }
};
