"use strict";

const concat = require("../../../../../../src/doc").builders.concat;

module.exports = {
  languages: [
    {
      name: "foo",
      parsers: ["foo"]
    }
  ],
  parsers: {
    foo: {
      parse: text => ({ text }),
      astFormat: "foo"
    }
  },
  printers: {
    foo: {
      print: path => concat(["foo+", path.getValue().text])
    }
  }
};
